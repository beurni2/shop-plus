{
  "name": "@shop-plus/store-projection",
  "version": "0.0.1",
  "private": true,
  "description": "SP#001-B — THE ONE store producer: a pure fold from the storefront + listing event stream to one store's projection, by the slug a link names (no directory — SP-I05 amended 2026-09-17). Dependency-free; canon shapes never redefined.",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
