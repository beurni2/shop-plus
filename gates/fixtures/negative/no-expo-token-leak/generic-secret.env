# NEGATIVE FIXTURE (audit E3): a committed high-entropy secret literal assigned
# to a SECRET-named env var, with NO EXPO_TOKEN anywhere in the file. The gate
# MUST fail here — and it can only do so via the generic secret-literal pattern,
# which makes that pattern load-bearing (the EXPO patterns never match this file).
PAYMENT_WEBHOOK_SECRET=a7Gk2Qm9Zp4Rt8Wx1Bn6Cv3Ls5Df0HjY2uE4iO
