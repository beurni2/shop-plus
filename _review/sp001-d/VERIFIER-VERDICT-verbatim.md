# SP#001-D — FRESH-CONTEXT VERIFIER VERDICT (verbatim, never rewritten)

Fresh-context subagent for a 🔴 RED money+custody slice, no memory of the build conversation. Given: the SP#001-D scope, named condition ②, the four fixtures + the préséance suite, the FORBIDDEN set, the Ten Laws, and the D-only diff (`git diff 7823b5d..ff85268`). It read the pin, the diff, the DO worker, and ran the suite uncached itself. Copied exactly as returned:

---

VERDICT: PASS

**A. (observation) Forbidden set holds exactly.** `git diff --name-only 7823b5d..ff85268` returns only: `pnpm-lock.yaml`, `services/attribution-service/package.json`, `src/durable-lock-client.ts`, `src/index.ts` (+1 line, a barrel re-export at index.ts:17), `test/durable-lock-client.test.ts`, `test/premiere-commande-reelle.e2e.test.ts`. Zero commerce-core / checkout / journey / earnings / ledger / mocks files touched. The frozen primitives (`ledger.ts`, `quote-issuance.ts`, `fixtures.ts`) are byte-unchanged and merely consumed. `package.json` adds only two `workspace:*` dev deps (commerce-core, storefront-service); pnpm-lock adds the matching `link:` entries. No new event name minted anywhere in the delta.

**B. (observation) Named condition ② is satisfied — the e2e locks THROUGH the durable DO, never the in-memory book.** `premiere-commande-reelle.e2e.test.ts:41-46` constructs a real Miniflare over `dist-worker/attribution-lock-worker.mjs` with `durableObjects: { ATTRIBUTION_LOCK: 'AttributionLockDO' }`. `doFetch` (line 53-56) dispatches `mf.dispatchFetch(.../locks/${checkoutRef})` into the workerd-hosted DO. The lock is taken at line 99 via `lockThroughDurableAuthority(doFetch, …)`, and `lockedResellerId = lockOutcome.lock.resellerId` (line 101). `AttributionLockBook` is **not imported** in the e2e (grep confirms NONE); it appears only in `durable-lock-client.test.ts:103` as the byte-equivalence reference and in the pre-existing `lock.test.ts`. The DO-locked reseller flows unbroken: `lockedResellerId` → `issueQuote({attributionResellerId: lockedResellerId})` (line 120) → `quote.attributionResellerId` → obligation party. Independent proof the DO is real and durable: `attribution-lock-do.e2e.test.ts` (7 tests, incl. "LOCK SURVIVES RESTART" across a Miniflare restart) passed.

**C. (observation) All four fixtures + préséance suite are real invariant assertions.**
- quote-immutable (lines 181-191): asserts `productSubtotal === sellerNet + resellerNet + platformProductFeeRevenue`, `buyerTotal === productSubtotal + deliveryFee`, `amountPaidAtCheckout + amountDueAtDelivery === buyerTotal`, then a second `put` returns `ok:false / quote_id_exists`. These are non-tautological: every field is independently derived by the pinned `computeWaterfall` in `quote-issuance.ts:92`. Numbers reconcile to the franc: 11500 = 8500 + 2000 + 1000; 12500 = 11500 + 1000; 12500 + 0 = 12500.
- obligation-equals-locked (lines 193-204): asserts `reseller.party === reseller:${lockedResellerId}` **AND** `=== reseller:${quote.attributionResellerId}`, and `reseller.amount === quote.resellerNet`. `ledger.ts:124-133` copies `quote.sellerNet`/`quote.resellerNet` and never imports the waterfall — copied, not recomputed.
- money-boundary-honest (lines 206-214): asserts leg `status === 'held'` (not captured), `escrow.status === 'hold'`, `payoutRefs === []`, read AFTER confirmOrder — so confirm did not flip to captured/payout. Reseller sees NET (2000), gross (2500) never surfaces; no `gross` reference in the delta.
- préséance (lines 217-255): locked beats a different explicit `RIVAL-9999` (line 218-229), explicit resolves with no lock, arrival resolves with neither, and a presented-but-unresolved `GHOST-0000` → `attributed:false` + `reconciliation.alert.v1` + `JSON.stringify(resolution)` does not match `/platform|shop-plus/i` (line 253). Never the platform.

**D. (observation) durable-lock-client fails closed and is byte-identical to the book.** `durable-lock-client.ts:41-48` maps 200/`created|idempotent` to a `LockOutcome` built with the SHARED `attributionLockedEvent`; lines 51-66 map 409/`collision` via the SHARED `attributionCollisionAlert`; line 71 throws on any other response — no fabricated lock, no platform fallback. `durable-lock-client.test.ts:98-111` asserts `JSON.stringify(durable.lock) === JSON.stringify(inMemory.lock)` and same for `.event`; line 113-119 asserts a status-500 fetch `rejects.toThrow(/unexpected response/)`.

**E. (observation) No franc moves, no custody faked, no celebration/ML.** The paid leg is provider-`held`, aggregator stage `hold`, `payoutRefs:[]`; Séra is the certified `MockSeraEligibilityEmitter` seam (no real dispatch). Delta greps: no `première_vente`/celebration/confetti, no ranking/ML/inference, and the DO worker bundle contains zero `@platform/contracts`/`zod` (pure `decideLock` core, as claimed).

**F. (observation) Tests genuinely pass — I ran them.** `pnpm -w turbo run test --filter=@shop-plus/attribution-service --force` (0 cached): **43/43 passed across 7 files** — including `premiere-commande-reelle.e2e.test.ts` (8), `durable-lock-client.test.ts` (5), `attribution-lock-do.e2e.test.ts` (7). The narrated run printed the reconciling numbers (productSubtotal 11500 = 8500 + 2000 + 1000; checkout leg 12500 « held »; reseller[rs-seller-0001] 2000; state confirmed).

Rationale: This is a wiring-and-proof slice done correctly. The only production code added is a thin transport (`durable-lock-client.ts`) that re-points the checkout lock seam at the durable `AttributionLockDO`, reuses the shared canon-event builders, and fails closed. The e2e drives the first real order end-to-end on real SP#001-A/B aggregates, locks attribution through the actual workerd DO (not the in-memory book), and carries that DO-locked reseller verbatim into an immutable, franc-reconciling Quote and into copied settlement obligations, with the money boundary asserted HELD and no payout. Every fixture asserts the invariant it names against independently-derived values, the frozen commerce-core/checkout/ledger primitives are byte-unchanged, and I ran the suite uncached to 43/43 green. No blocker found on any of the ten laws, the forbidden set, or named condition ②.

---

**CTO note:** clean PASS, zero blockers, zero non-blocking flags — every finding is confirmatory. The verifier independently confirmed the WIRED path (the e2e does not import `AttributionLockBook`; the DO-locked reseller flows unbroken into the Quote and obligations), the franc-exact reconciliation, the held money boundary with no payout, and the fail-closed lock client — and ran the suite uncached to 43/43.
